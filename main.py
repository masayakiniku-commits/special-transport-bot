import os
import requests

BEARER_TOKEN = os.getenv("X_BEARER_TOKEN")
LINE_TOKEN = os.getenv("LINE_TOKEN")

def send_line(msg):
print("LINE送信テスト")
return

def main():
print("起動OK")

if **name** == "**main**":
main()
